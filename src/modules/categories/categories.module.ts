import { Module } from '@nestjs/common';

/**
 * Product categories / taxonomy.
 *
 * Placeholder module — build it out following the products blueprint:
 * `entities/`, `dto/`, `categories.service.ts`, `categories.controller.ts`,
 * then register the controller/provider here.
 */
@Module({
  controllers: [],
  providers: [],
  exports: [],
})
export class CategoriesModule {}
