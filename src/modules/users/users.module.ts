import { Module } from '@nestjs/common';

/**
 * Customer & account management.
 *
 * Placeholder module — build it out following the products blueprint:
 * `entities/`, `dto/`, `users.service.ts`, `users.controller.ts`.
 * Export `UsersService` so the auth module can resolve credentials.
 */
@Module({
  controllers: [],
  providers: [],
  exports: [],
})
export class UsersModule {}
